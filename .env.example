# === AI APIs ===
ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_API_KEY=sk-or-...

# === Scraping ===
APIFY_API_TOKEN=apify_api_...

# === Telegram — Commander Bot (handles incoming /go /status commands) ===
TELEGRAM_BOT_TOKEN=...
TELEGRAM_GROUP_CHAT_ID=-100...      # Agent discussion group
TELEGRAM_APPROVAL_CHAT_ID=...       # Your personal chat for approvals

# === Telegram — Agent Bots (each sends messages as themselves) ===
# Create via @BotFather, add all 4 bots to TELEGRAM_GROUP_CHAT_ID group
ARIA_BOT_TOKEN=...                  # Trend Analyst
MAX_BOT_TOKEN=...                   # Copywriter
ZOE_BOT_TOKEN=...                   # Image Gen / Visual Director
CHIEF_BOT_TOKEN=...                 # Supervisor / Content Director

# === N8N Integration ===
N8N_BASE_URL=https://your-n8n.zeabur.app   # N8N instance URL

# === Database ===
DATABASE_URL=sqlite+aiosqlite:///./lazydog.db

# === Server ===
BACKEND_URL=https://your-ngrok-url.ngrok-free.dev    # Used to register TG webhook (ngrok URL in dev)
